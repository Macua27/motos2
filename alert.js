// Pide al usuario su edad
let edad = prompt("Por favor, ingresa tu edad:");

// Verifica que la entrada no esté vacía y sea un número
if (edad) {
    alert("Tienes " + edad + " años.");
} else {
    alert("No ingresaste una edad válida.");
}
